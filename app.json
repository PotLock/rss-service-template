{
  "name": "RSS Service",
  "description": "A lightweight, scalable RSS feed service built with Hono.js and Upstash Redis",
  "repository": "https://github.com/potlock/rss-service-template",
  "logo": "https://raw.githubusercontent.com/potlock/rss-service-template/main/logo.png",
  "keywords": ["node", "rss", "feed", "hono", "upstash", "redis"],
  "env": {
    "API_SECRET": {
      "description": "Secret key for API authentication",
      "generator": "secret"
    },
    "UPSTASH_REDIS_REST_URL": {
      "description": "Upstash Redis REST URL (create at https://upstash.com)",
      "required": true
    },
    "UPSTASH_REDIS_REST_TOKEN": {
      "description": "Upstash Redis REST token (create at https://upstash.com)",
      "required": true
    },
    "ALLOWED_ORIGINS": {
      "description": "Comma-separated list of allowed origins for CORS",
      "required": false,
      "value": "*"
    },
    "NODE_ENV": {
      "description": "Environment mode (development/production)",
      "value": "production"
    }
  },
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "formation": {
    "web": {
      "quantity": 1,
      "size": "eco"
    }
  }
}
